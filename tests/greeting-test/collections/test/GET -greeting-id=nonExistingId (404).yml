info:
  name: GET /greeting?id=nonExistingId (404)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/greeting?id={{nonExistingId}}"
  params:
    - name: id
      value: "{{nonExistingId}}"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Статус 404 для GET /greeting?id={nonExistingId}", function() {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
