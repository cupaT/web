info:
  name: GET /greeting/{nonExistingId} (404)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/greeting/{{nonExistingId}}"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Статус 404 для GET /greeting/{nonExistingId}", function() {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
